#container.masonry
  = render :partial => 'cat', :collection => @cats
%nav#page-nav
  %a{:href => "/?page=#{params[:page].nil? ? 2 : params[:page].to_i + 1}"} next


-# %h1.catz CATS
-# #cats
-#   -# - @images.each do |url|
-#   -#   %p= image_tag url, :class => 'lazy'
-# 
-# :javascript
-#   $(document).ready(function() {
-#     $.ajax({
-#       type: 'GET',
-#       async: true,
-#       url: '/cats.json',
-#       success: function(data) {
-#         $.each(data, function(ndx, value) {
-#           $('#cats').append('<p><img class="lazy" src="' + value +'" /></p>');
-#         });
-#       }
-#     })
-#   });